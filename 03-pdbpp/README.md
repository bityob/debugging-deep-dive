# 03-pdbpp

```
# Run scrip with debugger attached
uv run python -m pdb debug_with_pdbpp.py

# enter sticky mode
(Pdb++) sticky

# Run and stop on unhandled exception
uv run python -m pdb debug_with_pdbpp2.py
```
